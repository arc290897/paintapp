import React from 'react';


const Heading = () =>{
	return(
		<h4 className="App" style={{paddingTop:"50px"}}>Select the gradient and then the box to change the colour</h4>
		)
}

export default Heading;